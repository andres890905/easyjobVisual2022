@model easyjob22.Models.Traslado

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Traslado</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdTraslados" />
            <input type="hidden" asp-for="Idusuarios" />
            <input type="hidden" asp-for="IdSucursalOrigen" />
            <div class="form-group">
                <label asp-for="IdSucursalDestino" class="control-label"></label>
                <select asp-for="IdSucursalDestino" class="form-control" asp-items="ViewBag.IdSucursalDestino"></select>
                <span asp-validation-for="IdSucursalDestino" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
