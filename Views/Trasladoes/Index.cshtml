@model IEnumerable<easyjob22.Models.Traslado>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdSucursalDestinoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdSucursalOrigenNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdusuariosNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdSucursalDestinoNavigation.IdSucursal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdSucursalOrigenNavigation.IdSucursal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdusuariosNavigation.Idusuarios)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
